# In local dev, leave this unset — Vite's dev-server proxy handles /api → localhost:8000.
# In production (Vercel), set this to your deployed Render backend URL, no trailing slash:
VITE_API_URL=https://your-backend.onrender.com
